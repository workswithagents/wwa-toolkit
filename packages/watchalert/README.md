# @wwa/watchalert

Watch files or directories for pattern changes and fire desktop notifications when matches are found.

## Usage

```bash
npx wwa watchalert --path ./logs --pattern "ERROR" --title "Error Detected"
```
