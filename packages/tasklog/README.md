# @wwa/tasklog

Minimal CLI time tracker. Logs tasks with start/stop timestamps to a single JSON file. No SaaS, no database.

## Usage

```bash
wwa tasklog start "my task"
wwa tasklog stop
wwa tasklog list
```
